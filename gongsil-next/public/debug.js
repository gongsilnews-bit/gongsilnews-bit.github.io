const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto('https://gongsilnews-bit.github.io/gongsil/index.html', {waitUntil: 'networkidle2'}); await page.click('.news-tab-item:nth-child(2)'); await new Promise(r => setTimeout(r, 2000)); const html = await page.$eval('.news-list', el => el.outerHTML); console.log(html); await browser.close(); })();
